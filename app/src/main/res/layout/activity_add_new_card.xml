<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/addCardRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/home_sheet_bg">

    <!-- ===== BACK BUTTON ===== -->
    <LinearLayout
        android:id="@+id/layoutBack"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:padding="4dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:layout_width="18dp"
            android:layout_height="18dp"
            android:src="@drawable/ic_chevron_left"
            android:contentDescription="Back"
            app:tint="#006ecd" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="2dp"
            android:text="@string/card_payment_back"
            android:fontFamily="@font/plus_jakarta_sans"
            android:textSize="15sp"
            android:textColor="#006ecd" />
    </LinearLayout>

    <!-- ===== ADD CARD BUTTON (pinned to bottom) ===== -->
    <LinearLayout
        android:id="@+id/btnAddCard"
        android:layout_width="0dp"
        android:layout_height="52dp"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="20dp"
        android:layout_marginBottom="32dp"
        android:gravity="center"
        android:orientation="horizontal"
        android:background="@drawable/bg_button_purple"
        android:clickable="true"
        android:focusable="true"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/textBtnAddCard"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/add_card_btn"
            android:fontFamily="@font/plus_jakarta_sans"
            android:textSize="15sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF" />

    </LinearLayout>

    <!-- ===== SCROLLABLE CONTENT ===== -->
    <androidx.core.widget.NestedScrollView
        android:id="@+id/scrollContent"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:fillViewport="true"
        app:layout_constraintTop_toBottomOf="@id/layoutBack"
        app:layout_constraintBottom_toTopOf="@id/btnAddCard"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="20dp"
            android:paddingEnd="20dp"
            android:paddingTop="20dp"
            android:paddingBottom="20dp">

            <!-- Page title -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/add_card_title"
                android:fontFamily="@font/plus_jakarta_sans"
                android:textSize="26sp"
                android:textStyle="bold"
                android:textColor="@color/home_section_title" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:text="@string/add_card_subtitle"
                android:fontFamily="@font/plus_jakarta_sans"
                android:textSize="14sp"
                android:textColor="@color/home_section_subtitle" />

            <!-- ── Card design picker ── -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="28dp"
                android:layout_marginBottom="10dp"
                android:text="@string/add_card_design_label"
                android:fontFamily="@font/plus_jakarta_sans"
                android:textSize="13sp"
                android:textStyle="bold"
                android:textColor="@color/home_section_title"
                android:letterSpacing="0.06" />

            <!-- Horizontal RecyclerView (negative horizontal padding to bleed to edges) -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/recyclerCardDesigns"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginStart="-10dp"
                android:layout_marginEnd="-10dp"
                android:clipToPadding="false"
                android:paddingStart="10dp"
                android:paddingEnd="10dp"
                android:orientation="horizontal"
                app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
                android:overScrollMode="never" />

            <!-- Divider -->
            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:layout_marginTop="24dp"
                android:layout_marginBottom="4dp"
                android:background="@color/home_divider" />

            <!-- ── Card details section ── -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:layout_marginBottom="4dp"
                android:text="@string/add_card_details_label"
                android:fontFamily="@font/plus_jakarta_sans"
                android:textSize="13sp"
                android:textStyle="bold"
                android:textColor="@color/home_section_title"
                android:letterSpacing="0.06" />

            <!-- Cardholder name -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:text="@string/add_card_field_name"
                android:fontFamily="@font/plus_jakarta_sans"
                android:textSize="13sp"
                android:textColor="@color/home_section_subtitle" />

            <EditText
                android:id="@+id/editHolderName"
                android:layout_width="match_parent"
                android:layout_height="50dp"
                android:layout_marginTop="6dp"
                android:background="@drawable/bg_auth_input"
                android:paddingStart="14dp"
                android:paddingEnd="14dp"
                android:inputType="textPersonName|textCapWords"
                android:fontFamily="@font/plus_jakarta_sans"
                android:textSize="15sp"
                android:textColor="@color/home_section_title"
                android:hint="@string/add_card_field_name_hint"
                android:maxLines="1"
                android:imeOptions="actionNext" />

            <!-- Initial balance -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:text="@string/add_card_field_balance"
                android:fontFamily="@font/plus_jakarta_sans"
                android:textSize="13sp"
                android:textColor="@color/home_section_subtitle" />

            <EditText
                android:id="@+id/editBalance"
                android:layout_width="match_parent"
                android:layout_height="50dp"
                android:layout_marginTop="6dp"
                android:background="@drawable/bg_auth_input"
                android:paddingStart="14dp"
                android:paddingEnd="14dp"
                android:inputType="numberDecimal"
                android:fontFamily="@font/plus_jakarta_sans"
                android:textSize="15sp"
                android:textColor="@color/home_section_title"
                android:hint="@string/add_card_field_balance_hint"
                android:maxLines="1"
                android:imeOptions="actionDone" />

        </LinearLayout>
    </androidx.core.widget.NestedScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>